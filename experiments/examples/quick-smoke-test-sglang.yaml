extends: "../base/sglang-defaults.yaml"

name: "quick-smoke-test-sglang"
description: "Quick smoke test with Qwen3-0.6B on SGLang — matches POC 3 specs (small model, cheap GPU)"

model:
  name: "Qwen/Qwen3-0.6B"

infrastructure:
  gpu_type: "RTX 4000 Ada"
  gpu_count: 1

workload:
  type: "single"
  requests:
    source: "../../prompts/baseline.jsonl"
    count: 10
  parameters:
    max_tokens: 128
    temperature: 0.7
